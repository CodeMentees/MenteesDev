# MenteesDev

# MenteesDev

Clone the repo
git clone https://github.com/CodeMentees/MenteesDev.git
cd  cd MenteesDev
Windows 
# install node and npm 
- winget search npm
- winget install OpenJS.NodeJS.LTS (22.0 for me)
- Allow yes, when sys promt appears
- close the terminal and open a new one
- node --version # v22.13.1
- npm --version # 10.9.2
# Install Vercel
- npm install -g vercel
- vercel --version # Vercel CLI 41.0.2

# Deploy with vercel 
vercel login
enter --> make sure you are logged in with the same github account in your default browser. 

-- la la la next to be continued. 